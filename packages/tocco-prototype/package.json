{
  "name": "tocco-prototype",
  "version": "0.1.49",
  "description": "first react prototype",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf dist",
    "compile": "babel-node ../../bin/compile",
    "deploy": "better-npm-run deploy",
    "prepublish": "better-npm-run deploy:prod"
  },
  "betterScripts": {
    "deploy": {
      "command": "npm run clean && npm run compile --package=tocco-prototype",
      "env": {
        "DEBUG": "app:*"
      }
    },
    "deploy:prod": {
      "command": "npm run deploy",
      "env": {
        "NODE_ENV": "production",
        "DEBUG": "app:*"
      }
    }
  },
  "peerDependencies": {
    "react": "^15.2.1",
    "react-dom": "^15.2.1"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-react": "^6.11.1",
    "better-npm-run": "0.0.9",
    "react": "latest",
    "react-dom": "latest",
    "react-redux": "^4.0.0",
    "react-router": "^2.5.2",
    "react-router-redux": "^4.0.5",
    "redux": "^3.0.0",
    "redux-form": "^5.3.1",
    "redux-saga": "^0.11.0",
    "redux-thunk": "^2.0.0",
    "rimraf": "^2.5.3",
    "tocco-ui": "^0.1.0"
  }
}