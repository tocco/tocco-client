import styled from 'styled-components'
import {scale} from 'tocco-ui'

export const StyledFormattedMessageWrapper = styled.div`
  font-size: ${scale.font(-1.5)};
  margin-top: ${scale.space(0.5)};
`

export const StyledBlockWrapper = styled.div`
  margin-top: ${scale.space(0.5)};
`

export const StyledLogo = styled.img`
  margin-top: ${scale.space(1)};
  width: 80%;
  height: auto;
`
