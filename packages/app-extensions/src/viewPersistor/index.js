import {persistViewInfo, clearPersistedViews} from './actions'
import {addToStore, viewInfoSelector} from './viewPersistor'

export default {persistViewInfo, addToStore, viewInfoSelector, clearPersistedViews}
