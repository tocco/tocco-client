import {setInput, inputChanged, INPUT_CHANGED, inputInitialized, INPUT_INITIALIZED} from './actions'
import reducer from './reducer'

export default reducer
export {setInput, inputChanged, INPUT_CHANGED, inputInitialized, INPUT_INITIALIZED}
